# 0.0.3

- Added audio search
- Added audio like/unlike support

# 0.0.2

- Splitted `+page.svelte` to components
  - Added `Tracklist` components
  - Added `Player` components

- Changed default page title to `Lixcy Player`
- Fixed set `prevAudio` is last track if current track is first track
- Removed backgound color from `PlayerMiddleActions` buttons when disabled
- Removed `@vime/core` and `@vime/svelte` depends

# 0.0.1

Initial release
